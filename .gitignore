# Python cache
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# macOS
.DS_Store

# Virtual environments
.venv/
venv/
env/

# Large / local experiment outputs — checkpoints and round data are too big for git.
# Metrics JSON + plots ARE tracked (they're tiny and belong in the paper artifact set).
experiments/checkpoints/
experiments/round_data/
experiments/*.log

# Weight / checkpoint blobs anywhere in the tree
*.safetensors
*.bin
*.pt
*.pth

# IDE
.idea/
.vscode/
*.swp
